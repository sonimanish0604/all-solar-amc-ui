name: Semgrep

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Skip until app code exists
        run: echo "Semgrep will be enabled after the Flutter app scaffold is added."
